# Demo Payments UI

Minimal pricing and checkout UI for the Stripe payment gateway demo.

## Scripts

- `pnpm install`
- `pnpm dev`
- `pnpm build`

The built assets are embedded by the parent Go service from `dist/`.
